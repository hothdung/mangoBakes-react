import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mango-bakes-cakes',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
